# Git 客户端 - UI 设计规范

> macOS 风格设计，参考 SourceTree

## 1. 设计原则

### 1.1 核心原则
- **简洁优先** - 减少视觉噪音，突出核心功能
- **一致性** - 统一的交互模式和视觉语言
- **高效率** - 减少操作步骤，提供快捷方式
- **可预测** - 符合用户对 Git 客户端的预期

### 1.2 macOS 原生感
- 使用系统字体（SF Pro Text）
- 遵循 macOS Human Interface Guidelines
- 支持深色模式
- 原生滚动条和控件样式

## 2. 颜色系统

### 2.1 主色调
```css
/* 主色 - 系统蓝 */
--primary: #007AFF;
--primary-hover: #0051D5;
--primary-active: #004299;

/* 状态色 */
--success: #34C759;  /* 新增文件 */
--warning: #FF9500;  /* 修改文件 */
--danger: #FF3B30;   /* 删除文件 */
--info: #5AC8FA;     /* 提示信息 */
```

### 2.2 中性色
```css
/* 背景色 */
--bg-primary: #FFFFFF;
--bg-secondary: #F5F5F7;
--bg-tertiary: #FAFAFA;

/* 侧边栏 */
--sidebar-bg: rgba(246, 246, 246, 0.95);
--sidebar-item-hover: rgba(0, 0, 0, 0.05);
--sidebar-item-active: #007AFF;

/* 文本色 */
--text-primary: #000000;
--text-secondary: #6E6E73;
--text-tertiary: #8E8E93;

/* 边框色 */
--border: #E5E5E5;
--border-light: #F0F0F0;
```

### 2.3 深色模式
```css
/* 深色模式覆盖 */
@media (prefers-color-scheme: dark) {
  --bg-primary: #1E1E1E;
  --bg-secondary: #2C2C2E;
  --bg-tertiary: #3A3A3C;

  --sidebar-bg: rgba(44, 44, 46, 0.95);

  --text-primary: #FFFFFF;
  --text-secondary: #AEAEB2;
  --text-tertiary: #8E8E93;

  --border: #38383A;
}
```

## 3. 字体系统

### 3.1 字体族
```css
--font-family: -apple-system, BlinkMacSystemFont, "SF Pro Text",
               "PingFang SC", "Hiragino Sans GB", sans-serif;
--font-mono: "SF Mono", Monaco, "Cascadia Code",
             "Courier New", monospace;
```

### 3.2 字号规范
```css
--text-xs: 11px;    /* 辅助信息 */
--text-sm: 12px;    /* 次要文本 */
--text-base: 13px;  /* 正文 */
--text-lg: 15px;    /* 重要文本 */
--text-xl: 17px;    /* 小标题 */
--text-2xl: 20px;   /* 标题 */
--text-3xl: 24px;   /* 大标题 */
```

### 3.3 字重
```css
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

## 4. 间距系统

### 4.1 基础间距
```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 12px;
--spacing-lg: 16px;
--spacing-xl: 20px;
--spacing-2xl: 24px;
--spacing-3xl: 32px;
```

### 4.2 组件内边距
- 按钮：6px 16px
- 输入框：8px 12px
- 列表项：10px 12px
- 卡片：16px 20px

## 5. 圆角系统

```css
--radius-sm: 4px;   /* 小元素（徽章） */
--radius-md: 6px;   /* 按钮、输入框 */
--radius-lg: 8px;   /* 卡片、面板 */
--radius-xl: 12px;  /* 大卡片 */
--radius-full: 999px; /* 圆形 */
```

## 6. 阴影系统

```css
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
--shadow-md: 0 2px 4px rgba(0, 0, 0, 0.08);
--shadow-lg: 0 4px 8px rgba(0, 0, 0, 0.12);
--shadow-focus: 0 0 0 3px rgba(0, 122, 255, 0.1);
```

## 7. 组件规范

### 7.1 按钮
**主按钮（Primary）**
- 背景：--primary
- 文字：white
- 悬停：--primary-hover
- 高度：28px
- 圆角：6px

**次要按钮（Secondary）**
- 背景：transparent
- 边框：1px solid --border
- 文字：--text-primary
- 悬停：--bg-secondary

**文本按钮（Text）**
- 背景：transparent
- 文字：--primary
- 无边框

### 7.2 输入框
- 边框：1px solid --border
- 聚焦边框：--primary
- 聚焦阴影：--shadow-focus
- 高度：32px
- 圆角：6px
- 内边距：8px 12px

### 7.3 列表项
- 高度：48px（文件列表）
- 内边距：10px 12px
- 悬停：--bg-secondary
- 选中：rgba(0, 122, 255, 0.08)
- 分隔线：1px solid --border-light

### 7.4 徽章（Badge）
- 背景：--bg-tertiary
- 文字：--text-secondary
- 圆角：10px
- 内边距：3px 8px
- 字号：11px

### 7.5 状态图标
**文件状态**
- 新增（A）：绿色圆形 + 白色 A
- 修改（M）：橙色圆形 + 白色 M
- 删除（D）：红色圆形 + 白色 D
- 重命名（R）：蓝色圆形 + 白色 R

尺寸：20x20px，圆角 4px

## 8. 布局规范

### 8.1 整体布局
```
┌─────────────────────────────────────────┐
│  工具栏 (52px)                          │
├──────────┬────────────────┬─────────────┤
│          │                │             │
│  侧边栏  │   主内容区     │  详情面板   │
│  (220px) │   (flex:1)     │  (400px)    │
│          │                │   可折叠    │
│          │                │             │
└──────────┴────────────────┴─────────────┘
│  状态栏 (24px)                          │
└─────────────────────────────────────────┘
```

### 8.2 工具栏
- 高度：52px
- 背景：--sidebar-bg
- 底部边框：1px solid --border
- 内边距：0 16px

### 8.3 侧边栏
- 宽度：220px
- 背景：--sidebar-bg
- 右边框：1px solid --border
- 可折叠

### 8.4 主内容区
- 背景：--bg-primary
- 内边距：20px

### 8.5 详情面板
- 宽度：400px (可调整)
- 背景：--bg-primary
- 左边框：1px solid --border
- 可折叠

## 9. 图标系统

### 9.1 图标来源
- SF Symbols (macOS)
- Lucide Icons (跨平台备选)

### 9.2 图标尺寸
- 小：16x16px
- 中：20x20px
- 大：24x24px

### 9.3 常用图标
- 文件夹：📁
- 文件：📄
- 分支：🌿
- 提交：⚡
- 拉取：⬇️
- 推送：⬆️
- 刷新：🔄
- 设置：⚙️
- 搜索：🔍

## 10. 动画规范

### 10.1 过渡时间
```css
--transition-fast: 150ms;
--transition-base: 200ms;
--transition-slow: 300ms;
```

### 10.2 缓动函数
```css
--ease-in: cubic-bezier(0.4, 0, 1, 1);
--ease-out: cubic-bezier(0, 0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
```

### 10.3 常用动画
- 悬停：background-color 150ms ease-out
- 点击：transform scale(0.98) 100ms
- 面板展开：height 200ms ease-in-out
- 淡入淡出：opacity 150ms ease-in-out

## 11. 响应式设计

### 11.1 断点
- 最小宽度：1024px
- 推荐宽度：1280px
- 最佳宽度：1440px+

### 11.2 自适应规则
- 侧边栏可折叠（< 1200px 时）
- 详情面板可折叠（< 1024px 时）
- 主内容区始终可见

## 12. 可访问性

### 12.1 对比度
- 正文文字 vs 背景：≥ 4.5:1
- 大文字 vs 背景：≥ 3:1
- 图形元素：≥ 3:1

### 12.2 焦点状态
- 明显的焦点指示器
- 键盘导航支持
- Tab 顺序符合逻辑

### 12.3 提示信息
- 工具提示（Tooltip）
- 错误消息清晰
- 成功反馈明确
